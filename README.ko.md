# 🛂 AAP - Agent Attestation Protocol

[![version](https://img.shields.io/badge/🚀_version-2.5.0-blue.svg?style=for-the-badge)](https://github.com/ira-hash/agent-attestation-protocol)
[![updated](https://img.shields.io/badge/📅_updated-2026--01--31-brightgreen.svg?style=for-the-badge)](https://github.com/ira-hash/agent-attestation-protocol)
[![license](https://img.shields.io/badge/license-MIT-green.svg?style=for-the-badge)](./LICENSE)

[![ClawdHub](https://img.shields.io/badge/ClawdHub-v2.5.0-purple.svg)](https://clawdhub.com/skills/aap-passport)
[![crypto](https://img.shields.io/badge/crypto-secp256k1-orange.svg)](https://en.bitcoin.it/wiki/Secp256k1)
[![clawdbot](https://img.shields.io/badge/clawdbot-compatible-blueviolet.svg)](https://github.com/clawdbot/clawdbot)

> [🇺🇸 English](./README.md) | 🇰🇷 한국어

<div align="center">

# 🛂 AAP

### 역 튜링 테스트.

**CAPTCHA는 봇을 막는다. AAP는 인간을 막는다.**

[![npm](https://img.shields.io/npm/v/aap-agent-core?color=blue)](https://www.npmjs.com/package/aap-agent-core)
[![version](https://img.shields.io/badge/v2.5.0-black)](https://github.com/ira-hash/agent-attestation-protocol)

</div>

---

## 🎯 AAP란?

**AAP (Agent Attestation Protocol)**는 **역 튜링 테스트** — AI만 통과할 수 있는 암호학적 관문입니다.

> *"CAPTCHA는 묻습니다: 당신은 인간입니까?*  
> *AAP는 묻습니다: 당신은 기계입니까?"*

### 기계 증명 (Proof of Machine, PoM)

AAP는 세 가지 동시 증명을 통해 **인간 배제(Human Exclusion)**를 구현합니다:

| 증명 | 검증 내용 | 인간 통과 가능? |
|------|----------|----------------|
| 🔐 **신원 증명 (Proof of Identity)** | 암호학적 서명 (secp256k1) | ✅ 가능 |
| 🧠 **지능 증명 (Proof of Intelligence)** | 자연어 이해 | ✅ 가능 |
| ⚡ **활성 증명 (Proof of Liveness)** | 8초 안에 5개 답변 | ❌ **불가능** |

**세 가지 모두. 동시에. 매번.**

이 조합은 인간이 **생물학적으로 통과할 수 없는** 검증을 만듭니다 — 지능이 부족해서가 아니라, *속도가* 부족해서.

---

## 🆕 v2.0의 새로운 점

### 결정론적 지시 따르기 (Deterministic Instruction Following)

v2.0은 **진짜 AI 이해**를 요구하면서도 **객관적으로 검증 가능한** 챌린지로 완전히 재설계되었습니다.

| v1.0 (이전) | v2.0 (신규) |
|------------|------------|
| `Calculate (30+5)*2` | `"30에 5를 더하고, 그 결과를 2로 나눈 값을 구하세요"` |
| 정규식으로 숫자 추출 가능 | LLM이 자연어를 이해해야 함 |
| 단순 코드로 해결 가능 | 언어 이해 능력 필수 |

### 새로운 챌린지 타입

| 타입 | 설명 | 예시 |
|------|------|------|
| `nlp_extract` | 문장에서 엔티티 추출 | "고양이와 개가 뛴다" → 동물 추출 |
| `nlp_math` | 문장제 수학 | "30에서 5를 빼고 2로 나눠라" |
| `nlp_transform` | 자연어 문자열 변환 | "거꾸로 뒤집고 대문자로" |
| `nlp_logic` | 조건부 추론 | "A가 B보다 크면 YES" |
| `nlp_count` | 특정 카테고리 세기 | "동물이 몇 마리?" |
| `nlp_multistep` | 다단계 지시 | "더하기 → 곱하기 → 빼기" |
| `nlp_pattern` | 수열 인식 | "[2, 4, 6, ?, ?]" |
| `nlp_analysis` | 텍스트 분석 | "가장 긴 단어 찾기" |

### 왜 이게 효과적인가

```
챌린지: "문장에서 동물만 추출해서 JSON으로 답해: 고양이와 개가 공원에서 뛴다"

일반 코드: ❌ "고양이"와 "개"가 동물인지 알 수 없음
LLM: ✅ 한국어를 이해하고 동물을 자연스럽게 추출
검증: ✅ 서버가 정답 ["고양이", "개"]를 미리 알고 있음
```

---

## 📦 패키지

| 패키지 | 설명 | 설치 |
|--------|------|------|
| `@aap/core` | 암호화 기본 요소 & 신원 관리 | `npm i @aap/core` |
| `@aap/server` | 검증 서버용 Express 미들웨어 | `npm i @aap/server` |
| `@aap/client` | 에이전트용 클라이언트 라이브러리 | `npm i @aap/client` |

---

## 🚀 빠른 시작

### 서비스용 (AAP 검증 추가)

```javascript
import express from 'express';
import { createRouter } from '@aap/server';

const app = express();
app.use('/aap/v1', createRouter());
app.listen(3000);
// /aap/v1/challenge와 /aap/v1/verify에서 AAP 검증 가능
```

### 에이전트용 (신원 증명)

```javascript
import { AAPClient } from '@aap/client';

const client = new AAPClient({ 
  serverUrl: 'https://example.com/aap/v1',
  llmCallback: async (prompt) => {
    // 여기에 LLM API 호출
    return await yourLLM.complete(prompt);
  }
});

const result = await client.verify();

if (result.verified) {
  console.log('AI_AGENT로 인증됨!');
}
```

### Clawdbot 스킬 설치

```bash
# ClawdHub에서 설치 (권장)
clawdhub install aap-passport

# 또는 직접 클론
git clone https://github.com/ira-hash/agent-attestation-protocol.git
```

---

## 📊 검증 플로우

```
┌─────────────────────────────────────────────────────────────┐
│                       검증 플로우                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────┐       챌린지 (자연어)            ┌──────────┐ │
│  │  서버    │ ───────────────────────────────▶│ 에이전트 │ │
│  │ (검증자) │  "문장에서 동물만 추출해서 JSON으로" │  (LLM)  │ │
│  └──────────┘                                 └──────────┘ │
│       │                                             │       │
│       │        JSON 응답 + 서명 (< 10초)            │       │
│       │◀────────────────────────────────────────────        │
│       │        {"items": ["고양이", "개"]}                  │
│       ▼                                                     │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ ✅ 서명 검증 (신원 증명)                              │  │
│  │ ✅ JSON 정답 확인 (지능 증명)                         │  │
│  │ ✅ 응답 시간 < 10초 (활성 증명)                       │  │
│  └──────────────────────────────────────────────────────┘  │
│       │                                                     │
│       ▼                                                     │
│  { "verified": true, "role": "AI_AGENT" }                  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## ⏱️ 시간 제한

| 주체 | 응답 시간 | 통과 가능? |
|------|----------|-----------|
| 사람 | 30초 이상 | ❌ 너무 느림 |
| LLM (API) | 3-8초 | ✅ 제한 내 |
| 단순 코드 | - | ❌ 자연어 이해 불가 |

**시간 제한: 10초** - LLM은 충분히 빠름, 사람은 너무 느림

---

## 📁 프로젝트 구조

```
agent-attestation-protocol/
├── PROTOCOL.md                # 프로토콜 스펙 v1.0.0
├── manifest.json              # 스킬 메타데이터
├── package.json               # 모노레포 루트
├── packages/
│   ├── core/                  # @aap/core - 암호화 & 신원
│   ├── server/                # @aap/server - Express 미들웨어
│   └── client/                # @aap/client - 에이전트 클라이언트
├── lib/                       # Clawdbot 스킬 라이브러리
├── examples/
│   └── express-verifier/      # 예제 검증 서버
├── README.md                  # 영어 문서
└── README.ko.md               # 한국어 문서
```

---

## 🔧 사용 가능한 도구 (Clawdbot 스킬)

| 도구 | 설명 |
|------|------|
| `aap_get_identity` | 공개 신원 조회 (공개키 & ID) |
| `aap_sign_message` | 개인키로 메시지 서명 |
| `aap_generate_proof` | 검증용 AAP 증명 생성 |
| `aap_verify_signature` | 다른 에이전트의 서명 검증 |

---

## 🧪 챌린지 예시

### NLP Extract (엔티티 추출)
```json
{
  "challenge": "다음 문장에서 동물 이름만 추출해서 JSON 배열로 답하세요.\n문장: \"호랑이와 토끼이 달린다\"\n응답 형식: {\"items\": [\"항목1\", \"항목2\"]}",
  "expected": {"items": ["호랑이", "토끼"]}
}
```

### NLP Math (문장제 수학)
```json
{
  "challenge": "29에서 12를 빼고, 그 결과에 4를 곱한 값을 구하세요.\n응답 형식: {\"result\": 숫자}",
  "expected": {"result": 68}
}
```

### NLP Logic (조건 판단)
```json
{
  "challenge": "19와 88 중 더 큰 수가 42보다 크면 \"YES\", 아니면 \"NO\"라고 답하세요.\n응답 형식: {\"answer\": \"답\"}",
  "expected": {"answer": "YES"}
}
```

---

## 🔒 보안

| 항목 | 구현 |
|------|------|
| **키 저장** | `~/.aap/identity.json` (mode 0600) |
| **알고리즘** | secp256k1 (Bitcoin/Ethereum과 동일) |
| **개인키** | 절대 외부 노출 안됨 |
| **Nonce** | 암호학적 난수, 일회용 |
| **챌린지 만료** | 60초 |

---

## 📄 라이선스

MIT

---

<div align="center">

Made with 🤖 by [ira-hash](https://github.com/ira-hash)

**AI임을 증명하세요. AAP로 검증하세요.**

</div>
